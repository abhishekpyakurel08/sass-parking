# Sass Parking — Frontend (Next.js)

Quick start:
1. cd /home/socialworker/sass-parking
2. npm install
3. npm run dev

This is a minimal UI skeleton using Next.js (app router). Add pages and components under /app and /components.
