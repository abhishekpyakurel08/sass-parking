PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb+srv://abhishek:zJft65VFbU7Oo5EG@sass-parking.9gp40wg.mongodb.net/

# Redis
REDIS_URL=redis://localhost:6379

# JWT Secrets
JWT_ACCESS_SECRET=access_super_secret_parking_saas_key_nepal_2026_99x#!!_access
JWT_REFRESH_SECRET=refresh_super_secret_parking_saas_key_nepal_2026_99x#!!_refresh
ACCESS_TOKEN_EXPIRES=15m
REFRESH_TOKEN_EXPIRES=7d

# Security
BCRYPT_ROUNDS=12
# For production, use comma-separated list of allowed origins
# For multi-tenant, you can use wildcard pattern: https://*.yourdomain.com
CORS_ORIGIN=http://localhost:3000,https://*.yourdomain.com

# Email Service (Resend)
RESEND_API_KEY=your_resend_api_key_here
FRONTEND_URL=http://localhost:3000

# Multi-Tenant Development (Optional)
# Set a default tenant slug for local development
DEV_TENANT_SLUG=

# Super Admin Seed
INITIAL_SUPERADMIN_EMAIL=admin@parkingsaas.com
INITIAL_SUPERADMIN_PASSWORD=SecureSuperAdminPass2026!